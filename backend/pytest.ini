[pytest]
# Tests import as `backend.app...`, so the repository root is the import root.
testpaths = tests
pythonpath = ..
asyncio_mode = auto
# Pinned explicitly: pytest-asyncio warns when this is unset and will change
# the default in a future release.
asyncio_default_fixture_loop_scope = function
