# SAHAY-AI backend development dependencies.
#
# STATUS: EXT-001 APPROVED and installed 2026-09-10 into backend/.venv.
#
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Test runner for the backend suite (role fan-out, consent gate, crisis path,
# human-decision separation, timeline leakage).
pytest==8.3.4
# Async test support for the WebSocket and database tests.
pytest-asyncio==0.25.0
# ASGI test client used by FastAPI's TestClient and for direct API calls.
httpx==0.28.1
# Lint and format. Chosen over flake8+black+isort to keep the toolchain small.
ruff==0.8.4
